torch>=2.9  # torch.optim.Muon was added in 2.9
numpy
datasketch
tokenizers       # subword tokenizer backends (Unigram / BPE)
transformers     # PreTrainedTokenizerFast wrapper + BabyLM eval interop
morfessor        # unsupervised morpheme segmentation for the `morph` tokenizer
