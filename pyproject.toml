[build-system]
requires = ["setuptools>=70", "pybind11>=2.12"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "transformers-tokenizer"
version = "0.1.0"
requires-python = ">=3.12"
